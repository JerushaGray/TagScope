[build-system]
requires = ["setuptools>=77.0"]
build-backend = "setuptools.build_meta"

[project]
name = "choopscoop"
version = "3.4.0"
description = "Playwright-powered site auditor and marketing tag detector."
readme = "README.md"
authors = [
    { name = "Jerusha Gray", email = "jerusha.gray@gmail.com" }
]
license = "MIT"
license-files = ["LICENSE.md"]
keywords = ["web-audit", "crawler", "tag-detection", "martech", "analytics", "playwright", "mcp"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
    "Topic :: Internet :: WWW/HTTP :: Site Management",
]
requires-python = ">=3.9"

dependencies = [
    "playwright>=1.44",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
mcp = ["mcp>=1.0"]
dev = ["pytest", "pytest-asyncio"]

[project.urls]
Homepage = "https://github.com/JerushaGray/ChoopScoop"
Issues = "https://github.com/JerushaGray/ChoopScoop/issues"

[project.scripts]
choopscoop = "choopscoop.cli:main"
choopscoop-mcp = "choopscoop.mcp_server:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
